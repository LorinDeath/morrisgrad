---
import Layout from '../layouts/Layout.astro';
import Notes from '../components/Notes.astro';
import MiniGame from '../components/MiniGame.astro';
import { Image } from 'astro:assets';
// ВАЖНО: Положи картинку в src/assets/lorin.png, иначе сайт выдаст ошибку
import lorinImg from '../assets/lorin.png';
---
<Layout title="МиниИгры | Моррис Град">
    <main class="container">
        <div class="title-wrapper">
            <div class="line line-left"></div>
            <h1>Мини-игры</h1>
            <div class="line line-right"></div>
        </div>

        <div class="shimmer-line"></div>

        <h2 class="center-title">ЗАГЛУШКА v0.2</h2>

        <div class="character-section">
            <div class="lorin-wrapper">
                <Image src={lorinImg} alt="Lorin" class="lorin-img" />
                <div class="glitch-overlay"></div>
            </div>
            <div class="comic-bubble">
                <p>Это всего лишь временная заглушка, чтобы удержать внимание пользователя на странице. Когда нибудь Алзор подлючит энергоблок к данной мини игре и игроки будут вырабатывать энергию для развития своего мини городка. Но это не точно.</p>
                <a href="/changelog-zaglushka" class="changelog-link">Список изменений ></a>
            </div>
        </div>
        
        <MiniGame />

        <div class="shimmer-line"></div>
        
        <div class="glitch-container">
            <h2 class="glitch-calm" data-text="В РАЗРАБОТКЕ!">В РАЗРАБОТКЕ!</h2>
        </div>

        <Notes />
    </main>
  
</Layout>

<style>
    .title-wrapper {
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 1.5rem;
        margin-top: 1rem;
        margin-bottom: 1rem;
    }

    .title-wrapper h1 {
        margin: 0;
        white-space: nowrap;
    }

    .line {
        flex: 1;
        height: 4px;
        background: var(--accent-gold, #ffd700);
        animation: pulse-gold 2s infinite ease-in-out;
    }

    .line-left {
        background: linear-gradient(90deg, transparent, var(--accent-gold, #ffd700));
        clip-path: polygon(0 45%, 100% 0, 100% 100%, 0 55%);
    }

    .line-right {
        background: linear-gradient(90deg, var(--accent-gold, #ffd700), transparent);
        clip-path: polygon(0 0, 100% 45%, 100% 55%, 0 100%);
    }

    @keyframes pulse-gold {
        0%, 100% { opacity: 0.6; filter: drop-shadow(0 0 3px var(--accent-gold, #ffd700)); }
        50% { opacity: 1; filter: drop-shadow(0 0 8px var(--accent-gold, #ffd700)); }
    }

    .center-title {
        text-align: center;
        margin-bottom: 2rem;
        color: var(--accent-gold, #ffd700);
    }

    .character-section {
        display: flex;
        align-items: flex-start;
        gap: 2rem;
        margin-bottom: 2rem;
        max-width: 900px;
        margin-left: auto;
        margin-right: auto;
    }

    /* Стили для Лорин (Слева) */
    .lorin-wrapper {
        position: relative;
        flex-shrink: 0;
        width: 150px;
        height: 150px;
        padding: 3px; /* Толщина градиентной рамки */
        background: linear-gradient(135deg, #a020f0, #ff0000); /* Фиолетово-красный градиент */
        border-radius: 12px;
        box-shadow: 0 0 20px rgba(160, 32, 240, 0.6), 0 0 40px rgba(255, 0, 0, 0.3); /* Свечение */
        overflow: hidden;
    }

    .lorin-img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        border-radius: 9px; /* Чуть меньше внешнего радиуса */
        display: block;
        position: relative;
        z-index: 1;
    }

    /* Лёгкий глитч поверх картинки (сканлайн) */
    .glitch-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(transparent 90%, rgba(255, 0, 255, 0.2) 95%, transparent 100%);
        background-size: 100% 200%;
        z-index: 2;
        pointer-events: none;
        animation: scanline 3s linear infinite;
        mix-blend-mode: overlay;
    }

    @keyframes scanline {
        0% { background-position: 0% 0%; }
        100% { background-position: 0% 200%; }
    }

    /* Диалоговое окно (Справа) */
    .comic-bubble {
        flex: 1;
        background: rgba(20, 20, 25, 0.9);
        border: 2px solid #444;
        border-radius: 15px;
        padding: 1.5rem;
        position: relative;
        box-shadow: 0 5px 15px rgba(0,0,0,0.5);
    }

    /* Хвостик облачка */
    .comic-bubble::before {
        content: '';
        position: absolute;
        left: -12px;
        top: 30px;
        width: 0;
        height: 0;
        border-top: 12px solid transparent;
        border-bottom: 12px solid transparent;
        border-right: 12px solid #444; /* Цвет рамки */
    }
    
    .comic-bubble::after {
        content: '';
        position: absolute;
        left: -9px;
        top: 30px;
        width: 0;
        height: 0;
        border-top: 12px solid transparent;
        border-bottom: 12px solid transparent;
        border-right: 12px solid rgba(20, 20, 25, 0.9); /* Цвет фона */
    }

    .comic-bubble p {
        margin: 0;
        line-height: 1.6;
        font-size: 1.1rem;
        color: #e0e0e0;
    }

    .changelog-link {
        display: inline-block;
        margin-top: 1rem;
        color: var(--accent-gold, #ffd700);
        font-weight: bold;
        text-decoration: none;
        border-bottom: 1px dashed var(--accent-gold, #ffd700);
    }
    .changelog-link:hover { color: #fff; border-color: #fff; }

    .glitch-container {
        text-align: center;
        margin: 3rem 0;
    }

    /* Спокойный глитч */
    .glitch-calm {
        font-size: 3rem;
        font-weight: 900;
        text-transform: uppercase;
        position: relative;
        color: #fff;
        letter-spacing: 0.1em;
        /* Статичное смещение для красоты (хроматическая аберрация) */
        text-shadow: 2px 0 rgba(255, 0, 0, 0.5), -2px 0 rgba(0, 255, 255, 0.5);
        animation: calm-pulse 3s infinite;
    }

    @keyframes calm-pulse {
        0%, 100% { opacity: 1; text-shadow: 2px 0 rgba(255, 0, 0, 0.5), -2px 0 rgba(0, 255, 255, 0.5); }
        50% { opacity: 0.9; text-shadow: 3px 0 rgba(255, 0, 0, 0.6), -3px 0 rgba(0, 255, 255, 0.6); }
        52% { opacity: 0.8; text-shadow: -2px 0 rgba(255, 0, 0, 0.6), 2px 0 rgba(0, 255, 255, 0.6); transform: skewX(2deg); }
        53% { opacity: 1; transform: skewX(0); }
    }

    @media (max-width: 600px) {
        .character-section {
            flex-direction: column;
            align-items: center;
        }
        .comic-bubble::before, .comic-bubble::after {
            /* Поворачиваем хвостик вверх для мобилок */
            left: 50%;
            top: -24px;
            transform: translateX(-50%) rotate(90deg);
        }
        .lorin-wrapper {
            margin-bottom: 1rem;
        }
    }
</style>
